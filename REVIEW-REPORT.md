# Overnight audit report — 2026-07-15/16

Branch: `review/coderabbit-full-repo` (local only, NOT pushed, NOT merged).
Six audit agents swept the whole codebase (data layer, security/RLS, sync
engine, UI/a11y, design-vs-docs, config/tests). Everything **safe** was fixed
in six commits below; everything **subjective or risky** is listed for you to
decide. 85 tests green (was 40), `tsc` clean after every batch.

---

## ⚠️ ACTION REQUIRED (before the iOS on-device sync test)

1. **Run `supabase/10-security-hardening.sql`** in the SQL Editor. It fixes:
   - `event.id` was `GENERATED ALWAYS` → every CSV import in sync mode was
     **silently erased** (Postgres rejects explicit ids → connector drops the
     ops → next checkpoint wipes the local rows). One-line `SET GENERATED BY
     DEFAULT` fixes the whole feature.
   - Email-match account linking trusted **unconfirmed** emails — with
     "Confirm email" OFF, anyone could sign up as one of the 11 legacy web
     users' addresses and inherit their tasks. The trigger + `03` backfill now
     require `email_confirmed_at`.
   - Any authenticated user could plant tasks in a *web* user's list via the
     unprotected legacy `user_id` column (column-level grants now exclude it).
   - Anyone could insert a completion row against a *victim's* recurring task
     (guessable sequential id) and permanently block their daily check-off via
     the unique constraint. WITH CHECK now requires ownership.
   - `avatars` bucket had no size/MIME caps = free public file hosting under
     your project domain. Now 1 MB, images only.
2. **"Confirm email" must stay ON, permanently.** It's currently ON (by
   accident, flagged 2026-07-11). Both the audit and CodeRabbit converged on
   this, and CodeRabbit sharpened it: with the toggle OFF, Supabase
   **auto-confirms signups** — `email_confirmed_at` gets set immediately — so
   the `10` migration's confirmed-email gate passes for an attacker too. The
   gate is only meaningful while the toggle is ON. As long as email-match
   account linking exists (the by-design migration path for the 11 legacy web
   users), confirm-email is a **security invariant**, not a preference.

---

## Fixed on this branch (6 commits)

| Commit | Batch | Highlights |
|---|---|---|
| `3e8c9d1` | Sync/security data-loss | supabase/10 written; connector per-op error handling (one poisoned op no longer discards the rest of an upload); SQLSTATE 22\* now permanent (a too-long title used to jam sync **forever**); monotonic id minting (bulk imports could collide in one millisecond); client-side length caps matching server varchars; CSV range validation (`2026-02-31` is a row error, not March 3); Daily double-submit guard |
| `b6e6a4f` | Auth lifecycle | Sign-out now **wipes the local sync database** (user B could read user A's tasks on the same device; A's queued offline writes replayed under B's JWT and were silently dropped), clears the query cache, disarms notifications; sign-in reconnects immediately; fresh installs wait for first sync instead of flashing "no tasks"; Android 13 notification-permission nag loop fixed |
| `07d60a4` | Optimistic mutations | Concurrent-safe rollback + last-one-settles invalidation across tasks/recurring/events (a failed swipe could wipe another in-flight swipe's state; a refetch could blink out a fresh quick-add); event delete/delete-all now roll back on error (they hid events forever on failure); recurring add swaps temp id → real row (toggling a just-added row silently no-op'd); duplicate recurring completion from another device no longer unchecks the row; dateless tasks sort last in Completed/Deleted; garbage day-page deep links fall back to today |
| `075810f` | UI/a11y | Tab bar was the Expo **template teal** (`#0a7ea4`), nav surfaces were stock RN palettes — now all from tokens (this was the one broken spot in the style-pack seam); reduced-motion now honored (design rule 5 hard-line) in list regroups + batch cascades; VoiceOver now hears status + due date on cards (it only heard the title); web double-click no longer completes/deletes twice; event-sheet delete no longer kills its own exit animation; form chips remount storm fixed; 44pt touch targets via hitSlop everywhere; grabber is a real Close button; collapsed sections hidden from screen readers; Android dialog-dismiss promise leak fixed; undo toast raised above the FAB (doc 02); pale CSV event colors get the pill lightness clamp for text |
| `09b6b11` + `0d2668f` | Config/cleanup | expo-image-picker permission strings (auto-defaults advertised camera+mic the app never uses — App Store review flag); `typecheck` script; unused `@powersync/react` removed; `@types/jest` matched to jest 29; `lib/supabase.web.ts` picks up Supabase email-link tokens on web; supabase/09 re-run could silently destroy the `recurring_completion` PK — now guarded; **all Expo template leftovers deleted** (modal route, ThemedText/View, parallax, hello-wave, `constants/theme.ts`, react logos) |
| `d3a7e10` | Tests | 45 new tests: id collision math, sync error classifier (would have caught both connector bugs), pill contrast clamps, row mappers, CSV validation, enter-marks. 12 suites / 85 tests |

**Visual changes to eyeball in the preview** (all intentional, all tiny):
active tab is now your indigo accent (was teal — biggest visible change);
"Empty trash" is red (irreversible ≠ the undoable "Clear all" next to it);
undo toasts sit above the FAB; event time text on colored events is a darker,
readable shade of the same hue; "New looks are coming soon." caption is
weight-500 (token) instead of 400.

---

## Deferred — needs your judgment (nothing here is broken)

**Design decisions (from the design audit — report-only per your instruction):**
1. **Urgent vs ongoing is color-alone** on cards and calendar dots (your
   non-negotiable rule 1 / WCAG 1.4.1). Overdue has its icon; urgent needs a
   non-color cue too — suggestion: small clock glyph next to the due date, or
   a doubled accent bar. Your call on the shape.
2. **Import sheet shows two filled buttons at once** ("Choose a different
   file" + "Import n events") — rule 3 violation. Suggestion: demote the first
   to a text link once a file is parsed; also its fill is the event blue,
   which doc 03 explicitly names as the generic-AI accent to avoid.
3. **Sheet shell is copy-pasted across 5 routes** (form/styles/import/help/
   event) with ad-hoc 220/280/260ms timings that aren't in doc 05's motion
   vocabulary. Suggestion: extract one `SheetShell`, canonize the tuned values
   as named motion tokens (like the swipe values were), and the reduced-motion
   + drag-to-dismiss behavior lands in all five at once.
4. **Three chip implementations with three geometries** (form 40pt, filter bar
   36pt, settings 40pt). One shared `Chip` in components/ would fix doc 02's
   "don't invent one-off styles" rule; hitSlop covers the a11y minimum today.
5. Smaller spec drifts, worth a doc-hygiene pass more than code changes:
   accent bar 3px vs doc's 2px (pick one, record it); 13pt font sizes (off the
   12/16 scale) on date chips + filter chips; calendar "+n" at 9pt; section
   chevrons swap instead of rotating; sync-status dot colors differ from doc
   02 (update the doc — the code is the newer decision); FAB→sheet
   choreography in doc 05 was never built (trim the doc or build it).
6. **Sign-in/sign-up design pass** (known placeholders): the audit's checklist
   when you get there — token radii/type, doc 02 primary button with in-place
   loading spinner, visible labels, focus states, error text under fields,
   and don't use `statusOngoingAccent` for the sign-up notice (status colors
   are for task state).
7. Styles sheet copy ("New looks for your tasks…") reads marketing-ish vs the
   app's tool voice — one factual line would fit doc 06 better.

**Technical, deliberately not done:**
8. **Session tokens live in plain AsyncStorage** — fine for dev; swap in the
   SecureStore adapter (Supabase's Expo guide) at the dev-build stage.
9. **`components/input-prompt.tsx` uses RN `<Modal>`** — the one sanctioned
   exception (static content only, documented inline). Convert to a
   transparentModal route during the Android pass if you want rule 3 absolute.
10. **Android keyboard double-compensation risk** in the form sheet (manual
    padding + `adjustResize`) — flagged for the Android pass checklist.
11. **Dot-separated CSV dates parse month-first** (`1.2.2026` = Jan 2); dots
    conventionally mean day-first in Europe. Either drop dot support or parse
    day-first — data-shape decision, left alone.
12. **List memoization** (React.memo on cards) — worth doing if typing in
    search ever janks on-device; measure first.
13. **"Today" goes stale across midnight** on Daily/calendar until a
    re-render; an AppState/interval tick would fix it. Cosmetic.
14. **Dropped sync ops are invisible** — the counter now exists
    (`getDroppedOpCount()` in the connector); surfacing it belongs to the
    sync-status indicator when you build it (docs/design/02).
15. `expo-notifications` plugin icon/color for Android (needs a monochrome
    icon asset — bundle with the Android pass).
16. Deleted-section tasks: **event colors on the calendar ring** still use the
    raw CSV color (only *text* got the clamp) — visually intentional, noting
    for completeness.

**Residual known edge (accepted):** if two task mutations are BOTH in flight
offline in REST mode and BOTH fail, the cache can keep one phantom change
until the next successful refetch. Sync mode (the real offline path) doesn't
have this — local writes don't fail. Fixing it fully needs per-entity inverse
rollback; not worth the complexity now.

---

## CodeRabbit

- CLI has **no Windows build**; it now lives in WSL2 Ubuntu (installed and
  authenticated tonight, Pro+ account). Invocation pattern is in memory +
  `.coderabbit.yaml` scopes reviews to app code (excludes `reference/`,
  `docs/`, assets, lockfile).
- The **first whole-repo run wedged WSL** hard enough that even `wsl --shutdown`
  hung; recovery = `taskkill /F /IM wsl.exe /IM wslhost.exe /IM wslrelay.exe`,
  then shutdown + fresh boot.
- **The scoped re-run completed: 47 findings (2 critical / 35 major / 10
  minor) over 203 files** — it diffed the review branch against main, so it
  double-checked the overnight fixes too. Outcome:
  - **Applied** (final commit): confirm-dialog fails closed on web; filter
    treats `''` as a real filter value; Daily pull-to-refresh includes the
    Schedule; "Trash emptied"/"Event deleted" toasts are success-gated (no
    undo exists for them); form Add button double-tap guard; batch cascade is
    single-flight; web hover-reset can no longer cancel a cascade exit;
    `parseWallClock` rejects rolled-over components; CSV rejects malformed
    END times; pill/event text now walks lightness until it truly clears
    **WCAG 4.5:1** against the composited background (saturated yellow was
    ~2.5:1 even after the old clamp); import rejects files >5 MB before
    loading into memory; sync init failure resets its partial state;
    sync-status hook catches init rejections; `supabase/09` repairs partial
    state + reconciles publication membership; `supabase/10` adds an
    `end_at >= start_at` check and documents the confirm-email caveat.
  - **Declined as false positives**: ~8 "respect reduced motion" findings on
    Reanimated-driven sheets/swipes — Reanimated ≥3.5 honors the OS setting
    by default (`ReduceMotion.System`); the imperative `LayoutAnimation` gap
    was the real violation and was already fixed in batch 4. Also: the web
    edge-zone hover design (intentional, developer-tuned), and the avatars /
    recurring-completion SQL findings (already fixed in `10`, which
    CodeRabbit read as unapplied because it diffs files, not the live DB).
  - **Deferred to your judgment** (added to the list above): dark-mode
    `textOnAccent` contrast (3.3:1 — but the accent is still the placeholder
    you plan to replace, so pick the pair together); splash-screen/font
    readiness management in the root layout; import/event sheets not
    scrollable on short screens or huge dynamic type; chunked CSV import is
    not atomic (needs an RPC or import-id + upsert); calendar year-view
    exposes years the month list can't scroll to (2021–2032 vs ±~2.5y);
    serializing notification reconciles; collapsed-section hydration race;
    email-based account linking as a concept (CodeRabbit rates it critical;
    it IS your documented, deliberate migration design — the confirm-email
    invariant is the compensating control).

---

## What was verified SOLID (the good news)

The agents explicitly confirmed: RLS policy coverage on all four tables (and
`app_users`/`verification_tokens` sealed); sync-rules bucket isolation; no
secrets anywhere in git history; the wall-clock date discipline (zero
`toISOString` on due dates, both modes); dual-mode behavioral parity op-by-op;
schema parity between client tables, sync rules, and SQL; the swipe engine's
motion values exactly matching doc 05's finalized numbers; the token system
matching doc 03 1:1; keyboard/leak/Dynamic-Type hygiene; and the anti-generic
checklist broadly passing. The failing themes were few and systemic — sync
lifecycle, reduced motion, touch targets, template leftovers — and those are
now fixed.
